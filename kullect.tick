var uptime = stream
    |from()
        .measurement('uptime.value')
stream
    // Select just the cpu measurement from our example database.
    |from()
        .measurement('cpu/usage_rate:')
    @kullect()
        .field('value')
        .uptime('uptime')
        .size(100)
        .as('cost')
    |httpOut('movingaverage')
    |influxDBOut()
        .database('k8s')
        .retentionPolicy('default')
        .measurement('cpu/usage')
        .tag('kapacitor', 'true')
        .tag('version', '0.2')
